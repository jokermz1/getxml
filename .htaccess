# Redirecionamento automático para a pasta public
<IfModule mod_rewrite.c>
    RewriteEngine On

    # Se não acessar /public diretamente, redirecionar
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>

# Opções de diretório
Options -Indexes
